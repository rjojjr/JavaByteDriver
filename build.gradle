

buildscript {
    repositories {
        mavenCentral()
        flatDir dirs: 'C:/Program Files/proguard/proguard6.1.1/lib'
    }

    dependencies {
        // classpath 'net.sf.proguard:proguard-gradle:6.1.1'
        classpath  ':proguard'
    }
}

plugins {
    id 'java'

}

//apply plugin: "io.freefair.lombok"

group 'com.kirchnersolutions.javabytedriver'
version '1.0.05a'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

jar {
    manifest {
        attributes(
                'Class-Path': configurations.compile.collect{it.toURI().toString()}.join(' '))
    }
}